packages:
  - apps/*
  - packages/*

# Allow the native build scripts needed for Angular's build/dev toolchain to run.
strictDepBuilds: false

allowBuilds:
  esbuild: true
  "@parcel/watcher": true
  lmdb: true
  msgpackr-extract: true
