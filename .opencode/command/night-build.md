---
description: Run the autonomous QCardWars overnight implementation loop.
agent: orchestrator
model: local/tiel-medium
---

Read `START_PROMPT.md` and execute it now. Treat it as an implementation mandate, not a planning request.
Continue autonomously through the acceptance checklist. Use `dev-scout`, `coder`, and `reviewer` sequentially;
never parallelize subagents. Do not ask the user routine questions. Leave the repository bootable and verified.

$ARGUMENTS
